# Legacy DeerFlow Reference

This directory contains the original DeerFlow chat implementation and supporting modules. It is excluded from the build (`tsconfig.json`) and kept only as reference material while the new Cognitive Cockpit architecture is implemented.

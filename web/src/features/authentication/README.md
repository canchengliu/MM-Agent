# Authentication Feature

This feature contains the UI components and logic for user authentication, including login and registration forms.

- `LoginForm.tsx`: Handles user login.
- `RegisterForm.tsx`: Handles new user registration.
